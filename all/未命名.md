```mermaid
graph TD
   erDiagram
    USER {
        int user_id PK, "用户ID，主键自增"
        varchar username, "用户名，唯一，登录用"
        varchar password, "密码，加密存储"
        varchar gender, "性别，0未知1男2女"
        tinyint age, "年龄"
        text occupation, "职业"
        text individual_constitution, "体质类型"
        text taste_preference, "口味偏好"
        varchar phone, "手机号"
        varchar address, "收货地址"
        varchar role, "用户角色"
        timestamp created_time, "注册时间"
        timestamp updated_time, "更新时间"
    }
    MERCHANT {
        bigint merchant_id PK, "商家ID，主键"
        varchar merchant_name, "商家名称"
        varchar contact_person, "联系人"
        varchar contact_phone, "联系电话"
        varchar business_license_no, "营业执照号"
        varchar license_image, "营业执照图片URL"
        varchar business_address, "经营地址"
        text business_scope, "经营范围"
        text merchant_intro, "商家介绍"
        datetime audit_time, "审核时间"
        bigint audit_admin_id FK, "审核管理员ID，外键"
        varchar audit_remark, "审核备注"
        datetime create_time, "创建时间"
        varchar merchant_status, "商家状态"
    }
    MEDICINAL_MATERIAL {
        int material_id PK, "药材ID，主键"
        varchar material_name, "药材名称"
        varchar material_alias, "药材别名"
        int category_id FK, "分类ID，外键"
        varchar image, "药材图片URL"
        varchar property_flavor, "性味归经"
        varchar efficacy, "功效"
        text indication, "主治症状"
        text origin_places, "产地"
    }
    HEALTH_KNOWLEDGE {
        int knowledge_id PK, "知识ID，主键自增"
        varchar title, "知识标题"
        int category_id FK, "分类ID，外键"
        longtext content, "知识图文详情"
        varchar suitable_for, "适宜人群"
        varchar efficacy, "功效"
        text usage_method, "用法用量"
        varchar related_products, "关联产品ID，JSON数组"
        tinyint audit_status, "审核状态0草稿1待审2通过3驳回"
        timestamp created_time, "发布时间"
        timestamp updated_time, "更新时间"
    }
    KNOWLEDGE_CATEGORY {
        int category_id PK, "分类ID，主键自增"
        varchar category_name, "分类名称"
        text category_desc, "分类描述"
    }
    PRODUCT_CATEGORY {
        int category_id PK, "产品分类ID，主键自增"
        varchar category_name, "产品分类名称（药膳/药材/茶饮）"
    }
    PRODUCT {
        int product_id PK, "产品ID，主键自增"
        int category_id FK, "产品分类ID，外键"
        varchar product_name, "产品名称"
        varchar product_image, "产品图片URL"
        text origin_places, "产地"
        varchar property_flavor, "性味归经"
        varchar efficacy, "主要功效"
        text contraindications, "食用禁忌/主治症状"
        decimal price, "产品价格"
        int stock, "库存数量"
        bigint merchant_id FK, "所属商家ID，外键"
    }
    SHOPPING_CART {
        int cart_id PK, "购物车ID，主键自增"
        int user_id FK, "用户ID，外键"
        int product_id FK, "商品ID，外键"
        int quantity, "商品数量"
        varchar product_name, "商品名称"
        decimal payment, "商品单价"
    }
    ORDER {
        varchar order_id PK, "订单编号，主键（年月日+随机数）"
        int user_id FK, "用户ID，外键"
        varchar nickname, "用户昵称"
        bigint merchant_id FK, "商家ID，外键"
        int quantity, "产品总数量"
        decimal total_amount, "订单总金额"
        tinyint payment_method, "支付方式1微信2支付宝3银行卡"
        timestamp created_time, "下单时间"
        varchar order_status, "订单状态"
    }
    ADMIN {
        int admin_id PK, "管理员ID，主键自增"
        varchar username, "账号，唯一"
        varchar password, "密码，加密存储"
        varchar real_name, "姓名"
        varchar role_name, "角色，唯一"
        tinyint data_scope, "数据权限1全部2本人"
    }

    ADMIN ||--o{ USER : "管理"
    ADMIN ||--o{ MERCHANT : "审核/管理"
    ADMIN ||--o{ HEALTH_KNOWLEDGE : "发布/审核/管理"
    ADMIN ||--o{ PRODUCT : "审核/管理"
    KNOWLEDGE_CATEGORY ||--o{ HEALTH_KNOWLEDGE : "包含"
    PRODUCT_CATEGORY ||--o{ PRODUCT : "包含"
    MERCHANT ||--o{ PRODUCT : "发布/管理"
    MERCHANT ||--o{ ORDER : "处理"
    USER ||--o{ SHOPPING_CART : "拥有"
    USER ||--o{ ORDER : "创建"
    PRODUCT ||--o{ SHOPPING_CART : "加入"
    PRODUCT ||--o{ ORDER : "包含"
    MEDICINAL_MATERIAL ||--o{ HEALTH_KNOWLEDGE : "关联科普"
    MEDICINAL_MATERIAL ||--o{ PRODUCT : "作为原料"
    HEALTH_KNOWLEDGE ||--o{ PRODUCT : "关联推荐"
  
```